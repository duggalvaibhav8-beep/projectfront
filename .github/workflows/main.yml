name: first git

on:
  push:
    branches:
      - main

jobs:
  run-app:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - run: npm i 
      - run: npm run build 
      - run: pm2 start http-server --name vaibhav -- -p 4200 -d false 
